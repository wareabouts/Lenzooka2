import {useEffect} from 'react';import {Scene} from './Scene';import {useStore,exhibit,backup} from './store';
export function App(){const s=useStore();useEffect(()=>{s.load();},[]);return <main><header><h1>Lenzooka2</h1><span>FUJINON P14×16.5 · CIRCUIT ATLAS</span>{!exhibit&&<><button onClick={()=>s.set({edit:!s.edit})}>Edit placement</button><button onClick={backup}>Backup</button></>}<small>{s.saveState}</small></header>{s.error&&<aside>{s.error}<button onClick={s.save}>Retry save</button></aside>}{s.data&&<div style={{height:'85vh'}}><Scene onPin={()=>{}} onFace={()=>{}}/></div>}</main>}
